worker:
  custom_model: worker_model
  custom_model_config:
      use_meta_node: True
      gnn:
        input_dim: 21  # TODO use game_state
        hidden_dim: 64
        output_dim: 64
      lstm:
        hidden_size: 64
        num_layers: 2
        dropout: 0.0
        bidirectional: false
      policy:
        hidden_size: 16
        output_size: 9
      value:
        hidden_size: 16
        # output_size: 1